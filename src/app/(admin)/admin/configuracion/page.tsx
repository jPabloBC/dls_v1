import { EmptyState } from "@/components/ui/EmptyState";

export default function AdminSettingsPage() {
  return (
    <EmptyState
      title="Configuracion inicial"
      description="Aqui se agregaran ajustes de usuarios, roles, canales de contacto, WhatsApp Business y automatizaciones."
    />
  );
}
