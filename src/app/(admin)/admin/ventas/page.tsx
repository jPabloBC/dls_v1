import { EmptyState } from "@/components/ui/EmptyState";

export default function AdminSalesPage() {
  return (
    <EmptyState
      title="Seguimiento de ventas pendiente"
      description="Modulo preparado para pipeline comercial, tareas, proximos contactos y oportunidades por etapa."
    />
  );
}
